import SwiftUI

// Create a dedicated grid line color
struct GridLines {
    // This specific RGB value is used to override the default divider color
    static let exactColor = Color(hue: 0, saturation: 0, brightness: 0.96078) // = 245/255
}
